# Video Splitter Environment Configuration
# Copy this file to .env and modify as needed

# Server Configuration
PORT=4000

# FFmpeg Paths (optional - will use system PATH if not specified)
# FFMPEG_PATH=C:\ffmpeg-7.1.1-full_build\bin\ffmpeg.exe
# FFPROBE_PATH=C:\ffmpeg-7.1.1-full_build\bin\ffprobe.exe

# Upload limits
MAX_FILE_SIZE=500MB

# Cleanup settings
CLEANUP_AFTER_HOURS=24
